# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.env.local
forge.yaml
forge.yaml.bak
forge.yaml.bak.*

# Secrets (e.g. GitHub App private keys used by scripts/refresh-bot-token.sh)
secrets/

# Claude Code
.claude/
CLAUDE.md

# ForgeDock local operational cache (GitHub artifacts remain authoritative)
.forgedock/state.db
.forgedock/state.db-*
.forgedock/worktrees/
.forgedock/tasks/
.forgedock/*.jsonl
.forgedock/*.stderr
.forgedock/observations/
.pi-subagents/

# Spec Knowledge Graph (generated; gitignored by default — opt-in to commit,
# mirroring per-repo adaptive scripts)
.forgedock/graph/

# Marketing (local only, not published to npm)
marketing/

# Node
node_modules/
/dist/
docs/site/.vitepress/dist/
.superpowers/

# Scratch diff dumps (root-level only — e.g. _bin_diff.txt, _cmd_diff.txt)
/_*_diff.txt
